import React from "react";
//import ProductsContext from "../../products-context";

const OutgoingPage = () => {
  //const { state, setState } = useContext(ProductsContext);
  //console.log(state);
  return <h1>OutgoingPage</h1>;
};

export default OutgoingPage;
